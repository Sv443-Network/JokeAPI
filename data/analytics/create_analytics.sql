CREATE TABLE analytics (
    aID INT(6) UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    aType VARCHAR(30) NOT NULL,
    aData LONGTEXT NOT NULL,
    aTimestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
)