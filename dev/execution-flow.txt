[Init]
    - Parse Jokes (make sure the format is correct)
    - Initialize Black- and Whitelists
    - Initialize Documentation Page
    - Start HTTP server

[On HTTP Request]
    > IF path matches a script in ./endpoints
        - execute that script
    > ELSE
        - return 404 error